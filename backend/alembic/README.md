# Alembic Migrations

This folder is reserved for database migrations when the backend moves from the in-memory demo store to Render Postgres.

Suggested next files:

```text
alembic.ini
alembic/env.py
alembic/versions/
```
